# PyTorch (ROCm) must be installed separately before the rest:
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/rocm7.2
openai-whisper
python-dotenv>=1.0.0
pytest>=7.0.0
